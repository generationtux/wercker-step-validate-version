name: validate-version
version: 0.0.4
description: A public step for use with Wercker CI that will validate the version of the build/container.
keywords:
  - validate
  - version
  - git
  - commit
  - build
  - docker
properties:
  version:
    type: string
    required: true
  url:
    type: string
    required: true
